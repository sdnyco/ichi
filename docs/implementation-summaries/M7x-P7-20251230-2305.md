# M7x P7 — compact last-seen timestamp on profile sheet

- `OtherProfileSheet` now renders the “Last seen …” badge using a compact formatter that flips to `Xd` once the elapsed time surpasses 24 hours, matching the wording already used on anchored cards in the place gallery.

