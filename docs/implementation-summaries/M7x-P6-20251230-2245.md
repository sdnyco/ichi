# M7x P6 — fix self-card check-in status label

- `ExpandedProfileSheet` now uses the existing `profile.checkin.statusSelf` locale string when rendering the self-card check-in banner, so the localized sentence (“You checked into…”) shows up instead of the raw key.

